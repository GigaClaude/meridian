# ── Docker users ──
# Only these vars are used by docker-compose.yml.
# Copy this file to .env and adjust as needed.

# Claude API (enables web UI chat — works with claude.ai or API platform keys)
ANTHROPIC_API_KEY=sk-ant-...

# Ollama — only set this if you already have Ollama running on the host
# OLLAMA_URL=http://host.docker.internal:11434

# Models — defaults work well, change if you prefer smaller/larger
GATEWAY_MODEL=qwen2.5-coder:14b
WORKER_MODEL=qwen2.5-coder:14b
EMBED_MODEL=nomic-embed-text

# Web UI port (change if 7891 conflicts)
# MERIDIAN_PORT=7891

# Agent identity — scopes checkpoints and briefings per agent for multi-agent setups
# Each agent gets its own state. Leave unset for single-agent use.
# MERIDIAN_AGENT_ID=giga

# ── Manual install users ──
# These are also used when running without Docker.

# Core
MERIDIAN_DATA_DIR=~/.meridian
MERIDIAN_PROJECT_ID=default
MERIDIAN_WORKING_DIR=~

# Service URLs (Docker sets these automatically via compose networking)
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=memories
OLLAMA_URL=http://localhost:11434

# Web Server
MERIDIAN_HOST=0.0.0.0
MERIDIAN_PORT=7891

# Tuning
HOT_MEMORY_MAX_TOKENS=2000
RECALL_DEFAULT_MAX_TOKENS=800
GATEWAY_SYNTHESIS_MAX_TOKENS=1000
EPISODIC_RETENTION_DAYS=90
COMPACT_THRESHOLD_PERCENT=70
